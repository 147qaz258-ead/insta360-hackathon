# 视障多感官影像产品

**产品说明文档 V0.2**

> **当前目标**：先把“这个产品到底是什么、为什么存在、怎么用”讲清楚。

> **一句话**  
> 把一张已经存在的图片，从“只能被眼睛看”变成“可以被视障用户通过触觉和声音主动探索”的内容。

这份文档不讨论市场规模，不讨论商业模式，也不把未验证的内容写成事实。先解决产品定义。

## 1. 我们在解决什么问题

视障用户可以通过语音知道一张图片里“有什么”，但图片里的位置、距离、布局、前后关系等信息，通常需要被拆成一句一句的话。用户需要在脑子里重新拼回空间关系。

**我们要解决的不是“让 AI 再描述一遍图片”，而是让用户能自己去找：谁在左边、谁在右边、谁离谁更近、画面里有哪些区域。**

> **当前核心问题**  
> 图片里有一部分信息本来就是空间信息。只用语音传递时，它会被变成线性的描述。我们想给这部分信息一个空间化的入口。

## 2. 产品是什么

产品暂时定义为一套**多感官影像界面**。

输入是一张已经存在的图片，输出不是一段说明文字，而是一份可以被触摸和继续询问的影像。

| 步骤 | 系统做什么 | 结果 |
|---|---|---|
| 1 | 输入图片 | 来自相机、手机、影石设备或已有图库 |
| 2 | AI 拆解图片 | 识别人、物体、位置、轮廓和关系 |
| 3 | 生成触觉结构 | 把需要空间表达的信息映射到可刷新触点 |
| 4 | 触摸探索 | 用户通过手指获取位置和整体关系 |
| 5 | 语音补充 | 触摸到某个区域后，系统说明“这是什么、在做什么” |

## 3. 为什么不是只做语音

语音适合回答“是什么”。触觉要验证的是另一件事：能不能更直接地表达“在哪里、彼此什么关系”。

**如果触觉不能明显减少理解空间关系的成本，就不应该为了硬件而保留触觉。**

## 4. 用户怎么用

先用一个简单场景定义产品，不把所有功能放进去。

> **示例**  
> 系统收到一张三人合照。AI 找到三个人的位置。触觉面上出现三个对应区域。用户先通过手摸到三个人的相对位置，再触摸其中一个区域，系统语音说明：“左侧是 A，正在挥手。”

用户不是先听一整段图片摘要，而是先获得整体空间，再决定自己想继续了解哪一部分。

## 5. 产品里每个部分为什么存在

| 部分 | 存在理由 |
|---|---|
| 图片 | 产品处理的对象。我们不重新做拍摄链路，默认图片已经存在。 |
| AI | 把原始图片变成“人、物、位置、关系”等结构。 |
| 触觉 | 负责表达位置、布局、轮廓、距离等空间信息。 |
| 语音 | 负责表达人物、动作、颜色、文字、语义等信息。 |
| 可刷新硬件 | 让同一个设备可以显示不同图片，而不是每张图都做一块固定凸图。 |

## 6. 当前产品形态

当前不把产品定死成某一种外观。先把底层形态定为：

> **可刷新触觉影像终端**  
> 一个可以由程序控制的二维触觉矩阵。AI 生成触觉图后，终端把对应位置升起或改变高度。用户通过触摸获取空间信息，语音负责补充内容。

可采购或参考的现成形态包括刷新式触觉显示器、盲文点显模块、可编程针阵。比赛原型可以先用低分辨率矩阵验证，不需要一开始做高密度设备。

## 7. 目前已经成立的部分

- 视障用户会接触、拍摄、保存和分享图片。
- AI 可以把图片拆成人、物体、位置和关系。
- 触觉可以表达图形、位置和空间关系。
- 刷新式触觉显示器已经存在，也有可编程设备和模块。

## 8. 目前还没有成立的部分

- 触觉对“图片理解”是否比纯语音多提供了足够大的价值。
- 用户是否愿意为了图片使用一个独立触觉设备。
- 哪些图片信息最值得进入触觉层，哪些应该留给语音。
- 最终设备应该是独立终端、相机配件、手机配件，还是别的形态。

> **现在最大的断点**  
> 我们还需要证明：图片里有一类用户真的需要的信息，纯语音表达得不够好，而触觉能明显改善这件事。这个断点不成立，产品就要改。

## 9. 下一步只验证一件事

同一张图片、同一个任务，做两种交互：

- **A：只用语音描述。**
- **B：触觉表达空间 + 语音补充语义。**

比较四个结果：

- 完成时间
- 错误次数
- 需要重复询问的次数
- 用户能不能说清整体空间关系

**如果 B 没有明显增量，就不继续为触觉硬件找理由。**

## 10. 比赛原型怎么做

第一版只做一个小闭环，不做完整产品。

| 环节 | 第一版范围 |
|---|---|
| 输入 | 一张人物或物体关系明确的图片 |
| AI | 识别 3–5 个主要对象，并输出二维位置 |
| 硬件 | 4×4、6×6 或 8×8 可刷新触觉矩阵 |
| 交互 | 触摸某个区域，语音说出对象和动作 |
| 验证 | 让使用者根据触觉 + 语音说出对象位置关系 |

## 11. 现在不要做的事情

- 不要重新做影石已经有的拍摄、跟踪、自动构图和剪辑能力。
- 不要先做盲人社交平台。
- 不要先做高密度针阵。
- 不要把“触觉一定更好”写成结论。
- 不要用一堆论文代替产品逻辑。论文只用来证明事实。

## 12. 当前版本的一句话

> **产品定义**  
> 把图片里的空间信息变成可以直接触摸的结构，再用语音补充“这是什么”。目标不是让用户听懂一张图片，而是让用户自己去探索一张图片。
